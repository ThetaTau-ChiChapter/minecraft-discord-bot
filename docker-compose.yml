services:
  bot:
    container_name: discord-bot
    build: .
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - minecraft

networks:
  minecraft:
    external: true
    name: ${MINECRAFT_NETWORK}
